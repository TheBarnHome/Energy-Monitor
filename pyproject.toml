[project]
name = "energy-monitor"
version = "0.1.0"
description = "Home Assistant custom integration for battery SoC forecasting."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
